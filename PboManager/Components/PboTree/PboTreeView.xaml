<UserControl x:Class="PboManager.Components.PboTree.PboTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PboManager.Components.PboTree"
             xmlns:controls="clr-namespace:System.Windows.Controls;assembly=MultiSelectTreeView"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance local:PboTreeModel}">
    
    <controls:MultiSelectTreeView Name="TreeView" ItemsSource="{Binding Root}" VerticalRulers="True" >
        <controls:MultiSelectTreeView.Resources>
            <HierarchicalDataTemplate DataType="{x:Type local:PboTreeNode}" ItemsSource="{Binding Children}">
                <local:PboTreeNodeView/>
            </HierarchicalDataTemplate>
        </controls:MultiSelectTreeView.Resources>
    </controls:MultiSelectTreeView>
</UserControl>

